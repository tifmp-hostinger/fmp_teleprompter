# Copie para .env e preencha. Usado por `docker compose up`.
# A chave fica no servidor: o navegador nunca a recebe.
OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# AI_MODEL=            # vazio = melhor modelo disponível na conta
# AI_RATE_PER_MIN=10   # gerações por IP por minuto
